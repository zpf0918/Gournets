<header>
	<div class="navbar" role="navigation">
		<div class="container">
    	<div class="navbar-header">
      	<a class="navbar-brand" href="/">GOURMETS</a>
      </div>
      <div class="collapse navbar-collapse" >
				<ul class="nav navbar-nav">
			
					<li>
						<div class="form-group form-inline search-bar">
							 <%= render :partial => "jobs/search_bar"%>
						</div>
					</li>
				</ul>
        <ul class="nav navbar-nav navbar-right">
          <li>
              <% if !current_user %>
							<li><%= link_to("Register", new_user_registration_path) %> </li>
					    <li><%= link_to("Log in", new_user_session_path) %></l
              <% else %>
              <li class="dropdown">
                   <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      Hello！, <%= current_user.name %>
                   <b class="caret"></b>
                   </a>
               <ul class="dropdown-menu">
                 <% if current_user.admin? %>
                 <li> <%= link_to("admin", admin_jobs_path)%></li>
                 <% end %>
                 <li> <%= link_to("My jobs", account_jobs_path)%></li>
                 <li> <%= link_to("log out", destroy_user_session_path, method: :delete) %> </li>
              </ul>
             </li>
            <% end %>
          </li>
        </ul>

      </div>
     </div>
	</div>
 </header>
